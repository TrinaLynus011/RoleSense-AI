export const environment = {
  production: true,
  // Set BACKEND_URL to your Docker container's public URL (e.g. Railway / Render / Fly.io)
  // This is replaced at build time via angular.json fileReplacements
  apiUrl: 'REPLACE_WITH_BACKEND_URL/api',
};
