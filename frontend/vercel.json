{
  "version": 2,
  "buildCommand": "npm install && npm run build:prod",
  "outputDirectory": "dist/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
